/** Pinned tool versions the generated configs are known to work with. */
export interface ToolVersions {
  readonly oxlint: string;
  readonly oxfmt: string;
  readonly knip: string;
  readonly devConfig: string;
}
