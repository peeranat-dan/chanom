/**
 * Injected by each consumer's tsdown `define`, built from the authored markdown
 * under `skills/coding-standards/` (see `skills/build-skill-files.ts`).
 */
declare const __CODING_STANDARDS_FILES__: readonly {
  readonly path: string;
  readonly contents: string;
}[];
